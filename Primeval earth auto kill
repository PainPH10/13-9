-- Đặt LocalScript này vào StarterPlayer -> StarterPlayerScripts hoặc StarterGui

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Tạo ScreenGui chứa thông báo
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "UpdateNotificationGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Tạo khung nền chính (Main Frame)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 380, 0, 160)
mainFrame.Position = UDim2.new(0.5, -190, 0.5, -80)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- Bo tròn góc khung
local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 12)
uiCorner.Parent = mainFrame

-- Viền sáng nhẹ cho đẹp
local uiStroke = Instance.new("UIStroke")
uiStroke.Color = Color3.fromRGB(255, 170, 0)
uiStroke.Thickness = 2
uiStroke.Parent = mainFrame

-- Tiêu đề thông báo
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 40)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "⚠️ THÔNG BÁO UPDATE"
titleLabel.TextColor3 = Color3.fromRGB(255, 170, 0)
titleLabel.TextSize = 18
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Parent = mainFrame

-- Nội dung thông báo
local descLabel = Instance.new("TextLabel")
descLabel.Size = UDim2.new(1, -20, 0, 60)
descLabel.Position = UDim2.new(0, 10, 0, 40)
descLabel.BackgroundTransparency = 1
descLabel.Text = "Menu mình đã update và sửa, hiện tại các bản này đã cũ. Các bạn vào bio TT @0painph10 để lấy bản mới!"
descLabel.TextColor3 = Color3.fromRGB(240, 240, 240)
descLabel.TextSize = 14
descLabel.Font = Enum.Font.Gotham
descLabel.TextWrapped = true
descLabel.Parent = mainFrame

-- Nút Đóng (Close Button)
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 120, 0, 30)
closeButton.Position = UDim2.new(0.5, -60, 1, -40)
closeButton.BackgroundColor3 = Color3.fromRGB(255, 170, 0)
closeButton.Text = "ĐÃ HIỂU"
closeButton.TextColor3 = Color3.fromRGB(30, 30, 30)
closeButton.TextSize = 14
closeButton.Font = Enum.Font.GothamBold
closeButton.Parent = mainFrame

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 8)
buttonCorner.Parent = closeButton

-- Hiệu ứng khi bấm nút tắt bảng thông báo
closeButton.MouseButton1Click:Connect(function()
	screenGui:Destroy()
end)
